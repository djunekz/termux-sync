# Updated tsctl

# ... existing code ...

# Replace git fetch error handling with proper if statement
if ! git fetch; then
    echo "Error fetching from git"
    exit 1
fi

# ... more existing code ...

# Replace unalias command with proper if statement
if alias_name=$(alias | grep 'name-of-alias'); then
    unalias name-of-alias
else
    echo "Alias 'name-of-alias' not found"
fi

# ... more existing code ...

# Fix printf format strings
printf "Value: %s\n" "$variable"
# ... Repeat for other lines ...