#!/bin/bash

# Check for ShellCheck warnings SC2015 and SC2059

# SC2015: Not using `&&` or `||` in tests; use `[[` instead.
# SC2059: Is a double-quoted string missing the `$` character?

main() {
    echo "Starting tsctl script..."
    # Your code here
    # Example of using if statement correctly
    if [[ -f /path/to/file ]]; then
        echo "File exists"
    else
        echo "File does not exist"
    fi

    echo "Ending tsctl script."
}

main  # Call the main function
